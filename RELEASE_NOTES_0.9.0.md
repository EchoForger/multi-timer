# MultiTimer 0.9.0

## 新功能

- 新增不限数量的倒计时预设，支持搜索、排序、系统语义色、系统声音或静音、提前 1/5/10 分钟提醒，以及最多四个收藏预设。
- 番茄钟升级为完整循环：工作、短休息、长休息，以及可配置的长休息轮次；只有自然完成的工作阶段会增加轮次。
- 增加原生 iPhone 工程，提供“预设 / 计时器”双标签和移动端预设管理、计时控制与番茄设置。
- 增加收藏预设 Widget、当前计时器 Widget，以及锁屏与 Dynamic Island Live Activity。
- Live Activity 自动选择最近操作的运行中计时器，也可手动固定；结束操作会回到应用二次确认。
- 使用 CloudKit 私有数据库与 `CKSyncEngine` 同步预设、运行中计时器和设置；删除通过墓碑传播，离线时继续本地使用并在恢复后合并。

## 数据与隐私

- 不需要 MultiTimer 账号，不包含广告、内购、任务管理或社交功能。
- 专注历史、趋势、目标和徽章仍只保存在每台设备本地，不上传任务名称。
- 未登录 iCloud、同步不可用或构建缺少 entitlement 时，应用会明确显示本地模式或“同步已暂停”。

## 发布说明

- macOS 仍支持 macOS 13；`CKSyncEngine` 在 macOS 14 及更高版本启用，旧系统安全回退本地模式。
- iPhone 首版要求 iOS 18 或更高版本。
- iPad 与 Apple Watch 完整应用将在后续 0.9.x 加入；Live Activity 会在系统支持的 Apple Watch 和 Mac 表面按系统规则呈现。

# MultiTimer 产品需求与路线图

> 这是一个可勾选的候选需求池。把想做的项目从 `[ ]` 改成 `[x]` 即可；不想做的项目可以删除，或改成 `[-]` 表示明确放弃。

## 产品原则

- [ ] 始终是纯菜单栏应用，不在 Dock 中显示常驻图标。
- [ ] 界面优先使用原生 macOS 控件，并跟随系统强调色、深浅色和辅助功能设置。
- [ ] 不强制注册账户，不依赖云服务，不默认收集遥测数据。
- [ ] 核心计时功能离线可用，数据默认只保存在本机。
- [ ] 同时支持 DMG 和 Homebrew 安装，并根据安装来源选择更新方式。
- [ ] 所有新增功能都不得让“快速启动一个计时器”变慢或变复杂。
- [ ] 高级功能默认收起，保持主面板紧凑。

---

## P0：稳定性与基础体验

- [x] **登录时启动**：在设置中提供“登录时自动启动”，使用 macOS 原生登录项机制。
- [x] **通知权限状态**：在面板中检测通知权限；被拒绝时显示明确说明和“打开系统设置”按钮。
- [ ] **睡眠唤醒校正**：Mac 睡眠、合盖或锁屏后，唤醒时立即重新计算所有倒计时状态。
- [ ] **系统时间变化校正**：手动修改时间、切换时区或夏令时变化后，倒计时仍保持正确。
- [x] **异常退出恢复**：应用崩溃或强制退出后，重新启动能够恢复尚未结束的计时器。
- [x] **原子化配置写入**：写入临时文件后再替换正式配置，避免断电造成 JSON 损坏。
- [x] **配置版本迁移**：状态文件增加 schema version，未来升级时自动迁移旧数据。
- [ ] **损坏配置恢复**：配置无法解析时自动备份原文件，并恢复安全默认值。
- [x] **菜单栏图标自检**：启动后检测状态栏项目是否可见，并给出可恢复的诊断提示。
- [ ] **重复进程反馈**：第二次启动时不创建新实例，同时让现有菜单栏图标短暂高亮。
- [ ] **退出保护**：存在运行中计时器时退出应用，需要确认；可勾选“不再询问”。
- [ ] **Universal 2 构建**：同时支持 Apple Silicon 与 Intel Mac。
- [ ] **Apple 签名与公证**：完成 Developer ID 签名、Notarization 和 Stapling，移除 Gatekeeper 手动说明。
- [ ] **更新回滚**：DMG 自动更新失败时自动恢复旧版本，并保留可读的错误原因。
- [ ] **更新设置**：允许关闭启动检查，或选择“每天一次 / 每周一次 / 仅手动”。
- [ ] **跳过版本管理**：在设置中显示已跳过版本，并提供“重新显示该版本”操作。
- [ ] **更新通道**：支持 Stable 与 Beta 通道，默认只使用 Stable GitHub Release。

## P1：计时器核心能力

- [ ] **暂停与继续**：单个计时器可以暂停、继续，并正确持久化暂停状态。
- [ ] **重新开始**：运行中的计时器可一键从原始时长重新开始。
- [x] **复制计时器**：复制名称、原始时长与计时类型，立即创建一个新计时器。
- [x] **调整剩余时间**：允许直接编辑剩余时间，不局限于固定的增减按钮。
- [-] **固定按钮增减时间**：已由直接编辑剩余时间和目标时间取代。
- [ ] **拖拽排序**：运行中的计时器可以拖拽排序，顺序重启后保留。
- [x] **置顶计时器**：标记一个重点计时器，始终显示在列表顶部。
- [ ] **分组**：按工作、学习、烹饪等分组，并支持折叠。
- [ ] **颜色标签**：每个计时器可选择系统标签颜色，同时保持色盲可辨识。
- [ ] **备注字段**：计时器可附带一段可选备注，默认收起。
- [ ] **截止时间模式**：除“倒计时 25 分钟”外，支持“在 15:30 提醒我”。
- [ ] **延迟启动**：创建后在指定时间才开始倒计时。
- [ ] **计时器队列**：前一个完成后自动启动下一个。
- [ ] **循环计时器**：按固定次数或无限循环重复。
- [ ] **间隔序列**：支持 `工作 25 分钟 → 休息 5 分钟 → 重复 4 次`。
- [x] **秒表模式**：提供正向计时，可记录圈次。
- [ ] **超时继续计数**：到点后显示 `+02:15`，直到用户确认。
- [ ] **完成后保留**：可选择完成后保留在列表、自动移除或进入历史记录。
- [ ] **批量操作**：全部暂停、全部继续、全部增加时间、全部清除。
- [ ] **多选操作**：按住 Command 选择多个计时器后批量处理。
- [ ] **撤销删除**：删除或“已检查”后短时间内可以撤销。
- [ ] **自然语言时长**：支持输入 `1h 20m`、`90s`、`下午 3 点`。
- [ ] **快速算式**：自定义时长支持 `25*4`、`1.5h` 等简单表达式。
- [ ] **名称自动补全**：根据最近使用的计时器名称提供本地建议。
- [ ] **重复名称处理**：相同名称自动增加序号，也允许用户关闭该行为。

## P1：预设与工作流

- [ ] **预设拖拽排序**：直接拖拽快速预设，不再依赖文本编辑窗口。
- [ ] **预设右键菜单**：重命名、修改时长、复制和删除。
- [ ] **预设图标**：可为预设选择 SF Symbol，例如咖啡、学习、烹饪。
- [ ] **预设声音**：每个预设保存独立的完成声音。
- [ ] **预设组**：按场景切换一组快捷按钮，例如“工作日”“健身”“厨房”。
- [ ] **最近使用**：自动展示最近使用的 3 个时长和名称组合。
- [ ] **收藏模板**：将一个正在运行的计时器保存为模板。
- [ ] **导入与导出**：使用可读 JSON 导入导出预设、设置和工作流。
- [ ] **分享模板**：生成可复制的文本或 URL，别人打开后可确认导入。
- [ ] **内置模板库**：提供番茄钟、泡茶、会议、专注冲刺等少量高质量模板。

## P1：菜单栏与交互设计

- [x] **菜单栏显示剩余时间**：可选在图标旁显示最近到期计时器的 `12:34`。
- [x] **菜单栏显示数量**：可选显示正在运行的计时器数量。
- [ ] **菜单栏图标状态**：无任务、运行中、暂停、已到点使用不同但克制的状态。
- [x] **最近到期优先**：可选按剩余时间自动排序。
- [ ] **紧凑/标准密度**：提供两档界面密度，默认保持紧凑。
- [ ] **面板宽度设置**：允许小、中、大三档，长任务名可读但不产生独立窗口。
- [ ] **保持面板打开**：提供图钉按钮，让菜单栏 Popover 在操作其他应用时不自动关闭。
- [ ] **滚动位置保留**：关闭并再次打开面板时，恢复之前的滚动位置。
- [ ] **空状态设计**：没有计时器时提供简短引导和最近使用入口。
- [ ] **右键快捷菜单**：右键菜单栏图标直接开始常用预设、暂停全部或退出。
- [ ] **Option 点击行为**：Option 点击菜单栏图标可快速开始上一次计时。
- [ ] **双击菜单栏图标**：可配置为开始上一次计时或暂停/继续重点计时器。
- [ ] **键盘完整操作**：无需鼠标即可创建、编辑、延时、删除和确认计时器。
- [ ] **快捷键提示**：按钮悬停或菜单中显示对应快捷键。
- [ ] **全局快捷键**：自定义快捷键打开面板、快速开始、暂停重点计时器。
- [ ] **焦点管理**：打开面板自动聚焦名称输入框；关闭后不抢占其他应用焦点。
- [ ] **Escape 行为一致**：编辑时取消编辑，未编辑时关闭面板。
- [ ] **Command+Z 撤销**：支持撤销删除、改名、调整时间和修改预设。
- [ ] **触控板手势**：在任务卡片上左右滑动显示暂停、完成或删除操作。
- [ ] **上下文菜单**：任务卡片右键提供所有操作，兼顾辅助功能用户。

## P1：提醒与声音

- [ ] **完成声音选择**：提供系统声音列表，并支持“静音”。
- [ ] **每个计时器独立声音**：不同任务使用不同声音。
- [ ] **试听声音**：选择声音时可以立即试听。
- [ ] **自定义音量**：独立于系统主音量设置提醒音量上限。
- [ ] **重复提醒**：未确认时每隔指定分钟再次提醒，可设置最大次数。
- [ ] **持续提醒模式**：适合烹饪等场景，直到用户点击“已检查”。
- [ ] **勿扰模式处理**：检测通知可能被专注模式抑制，并给出非侵入说明。
- [ ] **菜单栏视觉提醒**：到点后图标轻微变化，不依赖声音或通知。
- [ ] **无障碍声音替代**：支持屏幕闪烁或菜单栏脉冲，且遵循“减少动态效果”。
- [ ] **通知操作扩展**：通知中提供 `+1 分钟`、`+5 分钟`、重新计时和已检查。
- [ ] **通知显示任务备注**：可选在通知正文中显示备注。
- [ ] **测试通知按钮**：设置页可发送测试通知和试听声音。

## P1：番茄钟

- [x] **独立番茄钟**：工作结束自动进入休息，休息结束可自动循环或等待手动开始。
- [x] **阶段控制**：支持暂停、继续、跳过当前阶段和停止。
- [x] **菜单栏状态**：工作与休息使用不同状态色，并显示固定宽度 `MM:SS`。
- [x] **番茄设置**：工作时长、休息时长、自动循环和模块显示开关即时保存。
- [x] **今日完成数量**：只统计自然完成的工作阶段，运行状态不跨重启恢复。
- [x] **番茄自动化**：提供 CLI 与 `multitimer://pomodoro/start` URL Scheme。
- [x] **阶段通知**：工作与休息使用不同提示音，通知可延长当前阶段 5 分钟。

## P2：历史、统计与专注

- [ ] **本地历史记录**：记录名称、计划时长、实际完成时间和操作结果。
- [ ] **历史记录开关**：默认关闭或首次明确询问，关闭时不保留完成记录。
- [ ] **历史快速重启**：从最近完成列表一键重新创建。
- [x] **番茄今日摘要**：显示今天自然完成的番茄工作阶段数量。
- [x] **番茄 30 天统计**：本地网页展示每日完成趋势，不记录任务名称。
- [ ] **周/月统计**：按任务名或分组汇总，不上传云端。
- [ ] **完成率**：区分正常完成、提前取消和超时确认。
- [ ] **连续专注记录**：以克制方式展示连续完成天数，可完全关闭。
- [ ] **目标设置**：例如每天完成 4 个专注计时，不使用游戏化惩罚。
- [x] **番茄 CSV 导出**：导出最近 30 天每日完成数量，便于个人分析。
- [ ] **JSON 导出**：完整备份所有历史与元数据。
- [ ] **自动清理历史**：可选择保留 7 天、30 天、1 年或永久。
- [ ] **隐私模式**：菜单栏和通知中隐藏任务名称，只显示剩余时间。

## P2：系统集成与自动化

- [x] **URL Scheme**：支持 `multitimer://start?name=Tea&minutes=5`。
- [x] **命令行工具**：提供 `multitimer start/list/pause/cancel`，与菜单栏实例通信。
- [ ] **macOS Shortcuts**：提供开始、暂停、延时、查询剩余时间等动作。
- [ ] **AppleScript 支持**：允许自动化创建和管理计时器。
- [ ] **Services/快速操作**：对选中文本使用“在 MultiTimer 中计时”。
- [ ] **Raycast 扩展**：快速搜索预设并开始计时。
- [ ] **Alfred Workflow**：提供轻量官方工作流。
- [ ] **Focus 模式联动**：开始特定预设时运行用户指定的快捷指令。
- [ ] **结束动作**：计时结束后可选运行一个 macOS 快捷指令，执行前明确授权。
- [ ] **日历时间块**：可选把计时记录写入用户指定日历，需要单独权限。
- [x] **菜单栏控制 API**：仅通过本地 Unix Socket 暴露最小控制接口；统计页按需绑定 `127.0.0.1`。
- [ ] **Stream Deck 支持文档**：通过快捷指令或 URL Scheme 配置实体按键。

## P2：辅助功能与国际化

- [ ] **VoiceOver 全覆盖**：所有按钮、进度、任务状态和剩余时间都有准确描述。
- [ ] **VoiceOver 操作顺序**：焦点顺序与视觉顺序一致。
- [ ] **全键盘导航测试**：开启系统键盘导航后无需鼠标完成核心流程。
- [ ] **增大文字支持**：系统辅助功能增大文字后不截断核心操作。
- [ ] **提高对比度支持**：遵循系统“提高对比度”设置。
- [ ] **减少透明度支持**：遵循“减少透明度”，提供清晰不透明背景。
- [ ] **减少动态效果支持**：关闭非必要动画和脉冲。
- [ ] **色盲友好**：状态不仅依赖颜色，同时使用文字或形状区分。
- [ ] **中文本地化**：统一简体中文术语、标点和时间格式。
- [ ] **英文本地化**：完整英文 UI、通知、更新窗口和官网。
- [x] **跟随系统语言**：支持 macOS“语言与地区”中的应用语言设置，应用内提供系统设置入口。
- [ ] **本地化长文本测试**：德语、法语等长文本不会破坏紧凑布局。
- [ ] **RTL 预研**：评估阿拉伯语、希伯来语布局，不要求首版实现。

## P2：设置与数据管理

- [ ] **原生设置面板**：仍由菜单栏进入，不创建 Dock 常驻图标。
- [ ] **常规设置**：登录启动、面板密度、默认名称和默认时长。
- [ ] **通知设置**：声音、重复提醒、通知正文和测试通知。
- [ ] **菜单栏设置**：显示图标、数量、最近剩余时间的组合。
- [ ] **快捷键设置**：检测冲突并支持恢复默认值。
- [ ] **更新设置**：检查频率、通道、跳过版本和手动检查。
- [ ] **数据位置展示**：明确显示配置路径，并提供“在 Finder 中显示”。
- [ ] **导出完整诊断包**：只包含用户确认的设置、版本与日志，不包含任务名称时可脱敏。
- [ ] **重置设置**：分为“仅重置偏好”和“清除所有本地数据”。
- [ ] **删除确认说明**：清楚列出即将删除的文件，并尽可能移动到废纸篓。

## P0/P1：安全、隐私与更新

- [ ] **最小权限清单**：文档列出通知、登录项、日历等权限及用途。
- [ ] **无网络核心模式**：关闭更新检查后，应用不产生任何网络请求。
- [ ] **更新来源固定**：只接受 `EchoForger/multi-timer` 的 GitHub Release。
- [ ] **Release 校验文件签名**：除 SHA-256 外，对校验清单进行开发者签名。
- [ ] **下载大小限制**：自动更新拒绝异常大的文件，并显示可理解错误。
- [ ] **重定向域名校验**：更新下载只允许 GitHub 官方域名与发布资源域名。
- [ ] **版本降级保护**：自动更新拒绝安装低于当前版本的包。
- [ ] **Bundle ID 校验**：替换前验证 App 的 bundle identifier 与预期一致。
- [ ] **代码签名身份校验**：正式签名后验证 Team ID，而不只验证签名结构。
- [x] **Brew 命令可见性**：更新确认窗口显示将执行的 Brew 操作摘要。
- [ ] **更新日志缓存**：网络失败时可显示上次成功获取的 Release 信息和时间。
- [ ] **隐私说明页面**：官网用简短语言说明无账户、无遥测和本地数据路径。
- [ ] **安全报告流程**：增加 `SECURITY.md` 和私下报告漏洞的方式。

## 工程质量与发布流程

- [ ] **逻辑单元测试**：覆盖版本比较、状态迁移、时长格式、跳过版本和更新路由。
- [ ] **更新器集成测试**：使用临时 App 与 DMG 验证安装、校验失败和回滚。
- [ ] **睡眠/时间变化测试**：增加可模拟时间源，避免依赖真实等待。
- [ ] **UI 快照测试**：浅色、深色、不同强调色、长名称和大量计时器。
- [ ] **辅助功能审计**：自动检查缺失的 accessibility label，并进行人工 VoiceOver 测试。
- [ ] **性能基准**：100 个计时器时 CPU、内存和每秒刷新开销可接受。
- [ ] **能耗检查**：空闲时避免不必要的每秒刷新和网络请求。
- [ ] **结构化日志**：使用轮转日志，默认不记录任务名称等私人内容。
- [ ] **GitHub Actions 测试**：每次提交运行语法、单元测试和格式检查。
- [ ] **自动构建 Release**：Tag 后自动构建、签名、公证、生成 DMG 和 SHA-256。
- [ ] **Homebrew 自动同步**：Release 成功后自动更新 cask，并验证真实下载哈希。
- [ ] **发布前冒烟测试**：菜单栏显示、启动计时、通知、重启恢复、检查更新。
- [ ] **版本一致性检查**：自动验证 Swift 源码、Info.plist、DMG、Release 与 cask 版本一致。
- [ ] **可复现构建记录**：记录 Swift、Xcode、macOS SDK 和依赖版本。
- [ ] **依赖安全扫描**：定期检查 SwiftPM 依赖与构建工具链漏洞。
- [ ] **Beta 发布流程**：Prerelease 不影响 Stable 用户，Beta 用户可主动加入。
- [ ] **崩溃诊断选择**：默认不上传；允许用户手动导出崩溃报告。

## 官网、README 与社区

- [ ] **官网动态版本号**：自动读取 GitHub Release，不手工维护。
- [ ] **下载按钮架构提示**：显示 Apple Silicon、Intel 或 Universal 信息。
- [ ] **更新流程说明**：分别演示 DMG 与 Brew 的确认和更新过程。
- [ ] **30 秒演示动画**：展示创建、并行计时、延时、改名和通知。
- [ ] **无障碍官网**：键盘导航、对比度、减少动画和语义结构通过检查。
- [ ] **FAQ**：菜单栏图标不见、通知不响、Gatekeeper、配置位置和卸载。
- [ ] **故障排查页**：提供不破坏数据的诊断步骤。
- [ ] **Release Notes 页面**：官网集中展示历史版本变化。
- [ ] **贡献指南**：增加 `CONTRIBUTING.md`、开发环境和 UI 规范。
- [ ] **行为准则**：增加 `CODE_OF_CONDUCT.md`。
- [ ] **Issue 模板**：Bug、功能建议和更新失败使用不同模板。
- [ ] **截图自动同步**：发布时自动生成浅色与深色截图并更新官网。
- [ ] **路线图公开**：README 链接到本文件，社区可通过 Issue 讨论候选项。

## 长期探索

- [ ] **原生 Swift 重写评估**：比较启动速度、包体积、签名、公证和维护成本，不立即重写。
- [ ] **Swift 辅助进程**：仅为 Shortcuts、登录项或系统集成提供最小原生桥接。
- [ ] **iPhone/Apple Watch 配套端**：只做本地网络或 iCloud 可选同步，不改变 Mac 离线核心。
- [x] **CloudKit 多设备同步**：使用私有数据库与 `CKSyncEngine` 同步预设、活跃计时器和设置，支持离线队列、冲突修订与删除墓碑；专注历史始终留在每台设备本机，ad-hoc 构建安全回退本地。
- [ ] **Live Activity / 灵动岛**：若开发移动端，显示重点计时器剩余时间。
- [ ] **Apple Watch Complication**：快速查看和延长重点计时器。
- [ ] **多人共享计时器**：仅作为独立可选功能预研，不引入强制账户。
- [ ] **插件机制**：优先使用 Shortcuts 和 URL Scheme，只有明确需求后再设计插件 API。

---

## 建议里程碑

### 0.4：可靠、可长期使用

- [ ] 完成通知权限状态与系统设置跳转。
- [ ] 完成睡眠唤醒、时间变化和异常退出恢复。
- [ ] 完成登录时启动。
- [ ] 完成暂停/继续、重新开始和拖拽排序。
- [ ] 完成更新设置与跳过版本管理。
- [ ] 建立基础单元测试和发布前冒烟测试。

### 0.5：高效工作流

- [ ] 完成计时器队列、循环和间隔序列。
- [ ] 完成全局快捷键与菜单栏剩余时间。
- [ ] 完成预设拖拽编辑、分组和导入导出。
- [ ] 完成声音选择、重复提醒和通知快捷操作。
- [ ] 完成 URL Scheme 与 macOS Shortcuts。

### 1.0：可信赖的原生开源应用

- [ ] 完成 Apple 签名、公证与 Universal 2。
- [ ] 完成 VoiceOver、键盘操作和系统辅助功能适配。
- [ ] 完成中英文完整本地化。
- [ ] 完成稳定的自动构建、Release 和 Homebrew 发布流程。
- [ ] 核心功能无已知数据丢失或菜单栏不可见问题。
- [ ] 官网、README、隐私说明、安全报告和贡献指南齐全。

## 每个版本发布前检查表

- [ ] 版本号在源码、spec、包信息、Release 和 Homebrew 中一致。
- [ ] 浅色、深色以及至少两种系统强调色显示正确。
- [ ] 菜单栏图标可见，Dock 中没有 MultiTimer。
- [ ] 创建、暂停、延时、改名、完成、取消和重启计时器正常。
- [ ] 通知权限允许与拒绝两种状态都有合理反馈。
- [ ] 应用重启后，计时器、预设、设置和跳过版本正确恢复。
- [ ] 启动检查与手动检查都能显示 GitHub Release 新版特性。
- [ ] “立即更新 / 晚点提醒我 / 跳过这个版本”行为正确。
- [ ] DMG 下载、SHA-256 校验、替换和失败回滚正常。
- [ ] Brew 更新使用正确 tap，失败信息可理解。
- [ ] DMG 已验证，Release 资产哈希与 cask 完全一致。
- [ ] 官网和 README 与当前界面及安装方式一致。

- [x] 根据目标时间设置计时器
- [x] 计时器每一项显示 剩余时间和 目标时间两个。两个都可以编辑，可以点击输入。比如剩余时间 1:00，可以点击 1，并接受键盘输入，比如输入 16，就会变成 16:00。有了这个之后可以直接去掉那些 +1 +10 的按钮
- [x] 记录改一下，本质上每一条就是记录开始时间和结束时间，在配置文件里面
- [x] 不需要显示进度条了，进度不重要。
- [x] 每次操作完都要按照剩余时间排序，最快结束的排在最上面
- [x] 输入计时器名称的那个文本框需要可以粘贴
- [x] 计时长度的那个 quick start 和 custom 都删掉，弄一个拉杆让用户输入计时长度。从 0 到 24h。或者在右侧输入
